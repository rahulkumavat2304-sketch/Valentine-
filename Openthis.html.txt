<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For Preetiii 💗</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
:root{
  --pink:#ff6f91;
  --soft:#ffe6ef;
  --dark:#444;
}

*{box-sizing:border-box}

body{
  margin:0;
  height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  background:
    radial-gradient(circle at 20% 20%, #ffd6e8, transparent 40%),
    radial-gradient(circle at 80% 80%, #ffc1d9, transparent 40%),
    linear-gradient(135deg,#fff0f6,#ffdbe9);
  font-family:'Comic Sans MS', cursive;
  overflow:hidden;
}

/* Card */
.card{
  background:white;
  border-radius:30px;
  padding:26px;
  text-align:center;
  width:min(92%,360px);
  box-shadow:0 25px 60px rgba(0,0,0,.18);
  animation:cardIn 1.2s ease;
  position:relative;
}

/* Soft glow */
.card::after{
  content:"";
  position:absolute;
  inset:-8px;
  border-radius:35px;
  background:linear-gradient(120deg,#ff9acb,#ffd6e8,#ff9acb);
  z-index:-1;
  filter:blur(18px);
  opacity:.6;
  animation:glow 4s ease-in-out infinite;
}

h1{color:var(--pink); margin:10px 0}
h2{color:#ff8fb3; margin:0}
p{color:var(--dark); font-size:15px; line-height:1.4}

/* Buttons */
.actions{
  margin-top:15px;
}

button{
  border:none;
  border-radius:999px;
  padding:12px 28px;
  font-size:17px;
  cursor:pointer;
}

#yes{
  background:var(--pink);
  color:white;
  box-shadow:0 8px 20px rgba(255,111,145,.45);
  animation:pulse 1.6s infinite;
}

#no{
  background:#eee;
  color:#666;
  position:absolute;
}

/* Tiny text */
.note{
  font-size:12px;
  opacity:.7;
  margin-top:8px;
}

/* Animations */
@keyframes cardIn{
  0%{transform:scale(.6) rotate(-3deg); opacity:0}
  100%{transform:scale(1); opacity:1}
}

@keyframes pulse{
  0%,100%{transform:scale(1)}
  50%{transform:scale(1.08)}
}

@keyframes glow{
  0%,100%{opacity:.4}
  50%{opacity:.8}
}

/* Floating hearts */
.heart{
  position:fixed;
  font-size:22px;
  animation:floatUp linear forwards;
  pointer-events:none;
}

@keyframes floatUp{
  from{transform:translateY(100vh) scale(.8); opacity:1}
  to{transform:translateY(-15vh) scale(1.3); opacity:0}
}

/* Confetti */
.confetti{
  position:fixed;
  width:8px;
  height:8px;
  background:var(--pink);
  animation:confettiFall 3s linear forwards;
}

@keyframes confettiFall{
  from{transform:translateY(-10vh) rotate(0deg)}
  to{transform:translateY(110vh) rotate(360deg)}
}
</style>
</head>

<body>

<div class="card" id="card">
  <h2>Hey Preetiii 🌸</h2>
  <h1>Will you be my Valentine? 💖</h1>

  <p>
    I don’t know how to impress perfectly,  
    but I know how to care honestly 🥺✨
  </p>

  <div class="actions">
    <button id="yes" onclick="yesClicked()">YES 💕</button>
    <button id="no" onmouseenter="runAway()">NO 🙈</button>
  </div>

  <div class="note">
    (little hint: NO is very shy today 😌)
  </div>
</div>

<audio id="music" loop>
  <source src="https://cdn.pixabay.com/audio/2023/03/09/audio_6c7e5d1d4c.mp3" type="audio/mpeg">
</audio>

<script>
const hearts = ["💖","💗","💞","💕"];

function runAway(){
  const no=document.getElementById("no");
  no.style.left=Math.random()*75+"vw";
  no.style.top=Math.random()*75+"vh";
}

function spawnHeart(){
  const h=document.createElement("div");
  h.className="heart";
  h.innerText=hearts[Math.floor(Math.random()*hearts.length)];
  h.style.left=Math.random()*100+"vw";
  h.style.animationDuration=(Math.random()*2+3)+"s";
  document.body.appendChild(h);
  setTimeout(()=>h.remove(),5000);
}

setInterval(spawnHeart,900);

function yesClicked(){
  document.getElementById("music").play().catch(()=>{});

  document.body.innerHTML=`
    <div class="card">
      <h1>She said YES 😭💗</h1>
      <p>
        Preetiii, you feel like comfort,  
        calm, and a soft smile all at once 🌙✨
      </p>
      <p>
        Thank you for choosing this moment with me 💞
      </p>
    </div>
  `;

  // Confetti
  for(let i=0;i<80;i++){
    const c=document.createElement("div");
    c.className="confetti";
    c.style.left=Math.random()*100+"vw";
    c.style.background=[
      "#ff6f91","#ffc2d1","#ff9acb","#ffd6e8"
    ][Math.floor(Math.random()*4)];
    c.style.animationDuration=(Math.random()*2+2)+"s";
    document.body.appendChild(c);
    setTimeout(()=>c.remove(),4000);
  }
}
</script>

</body>
</html>